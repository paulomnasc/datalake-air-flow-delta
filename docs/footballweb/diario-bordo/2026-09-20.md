# Diário de Bordo - 20/09/2026

## Calibração de UX e Relatório: Padrão Global do Filtro de Apostas Confirmadas na Tela "Eficiência de Palpites"

---

### 1. Motivação e Diagnóstico Técnico
Ao analisar a tela **"Eficiência de Palpites"** para o dia 19/09/2026, o usuário identificou que o card de segmentação **Total de Cartões (Over/Under)** apresentava:
* **0 entradas** / **Win Rate: 0%**.

Contudo, na tela **"Minhas Simulações de Apostas"**, foram registradas **7 apostas de cartões** no mesmo dia, com excelente acurácia: **6 Ganhas (85,7%) e 1 Perdida (14,3%)**.

#### Causa Raiz Identificada:
* A tela de "Minhas Simulações de Apostas" utiliza por padrão o filtro `Apostas Confirmadas: Todos`, trazendo tanto bilhetes com dinheiro real em conta corrente (`confirmada = 1`) quanto apostas automáticas de simulação pré-jogo da IA (`confirmada = 0`).
* Em contrapartida, a tela "Eficiência de Palpites" (`ApostaController::relatorioEficiencia`) possuía como valor padrão inicial `$confirmedFilter = '1'` (`Apostas Confirmadas: Sim`), restringindo as métricas de eficácia exclusivamente a apostas com confirmação de débito em conta corrente.
* Como os robôs de predição de cartões operam simulando palpites pré-jogo (`confirmada = 0`), e as 6 apostas confirmadas pelo usuário no dia eram de Handicap Asiático, a tela de Eficiência omitia os 7 palpites de cartões da IA, zerando as estatísticas do mercado.

---

### 2. Arquivos e Trechos Modificados
* **`src/footballweb/app/Controllers/ApostaController.php`**:
  * No método `relatorioEficiencia()`, o fallback da variável `$confirmedFilter` foi atualizado de `'1'` para `'all'`:
    ```php
    $confirmedFilter = $this->request->getVar('confirmed') ?? 'all';
    ```
  * Quando não especificado na requisição, o sistema busca todas as apostas analisadas (confirmadas e simulações pré-jogo da IA), calculando o desempenho global dos modelos preditivos.

* **`src/footballweb/app/Views/apostas/relatorio_eficiencia.php`**:
  * O componente de toggle deslizante (`#confirmedSlideToggle`) foi atualizado para marcar como ativo por padrão o botão **"Todas"** quando `$confirmedFilter` for `'all'` ou nulo/vazio, e o input oculto `confirmedFilterInput` recebe `'all'` por default:
    ```html
    <input type="hidden" name="confirmed" id="confirmedFilterInput" value="<?= esc($confirmedFilter ?? 'all') ?>">
    <button type="button" class="slide-btn <?= ($confirmedFilter === 'all' || empty($confirmedFilter)) ? 'active' : '' ?>" data-val="all" onclick="setEficienciaConfirmedFilter('all', this)" title="Exibir todas as apostas (confirmadas e não confirmadas)">Todas</button>
    <button type="button" class="slide-btn <?= ($confirmedFilter === '1') ? 'active' : '' ?>" data-val="1" onclick="setEficienciaConfirmedFilter('1', this)" title="Exibir apenas apostas confirmadas (com débito em conta)">Sim</button>
    <button type="button" class="slide-btn <?= ($confirmedFilter === '0') ? 'active-no' : '' ?>" data-val="0" onclick="setEficienciaConfirmedFilter('0', this)" title="Exibir apenas apostas não confirmadas">Não</button>
    ```

---

### 3. Impacto Prático e Regras de Negócio
| Cenário | Comportamento Anterior | Novo Comportamento |
| :--- | :--- | :--- |
| **Acesso Direto à Tela Eficiência de Palpites** | Carregava com filtro `Confirmadas = Sim`, ocultando palpites em modo simulação da IA | **Carrega com filtro `Confirmadas = Todas`**, exibindo a eficiência global de todos os palpites da IA (Handicap e Cartões) |
| **Avaliação da Acurácia dos Modelos Preditivos** | Restrito a apostas financeiramente executadas pelo usuário | **Transparência total** com todas as predições geradas (ex: 7 entradas de cartões com 85,7% de acerto em 19/09/2026) |
| **Filtro Específico sob Demanda** | Usuário podia filtrar | Usuário continua podendo filtrar para `Sim` ou `Não` a qualquer momento com um clique |

---

### 4. Validação e Testes
* **Validação Sintática PHP**:
  * `php -l src/footballweb/app/Controllers/ApostaController.php` ➔ **No syntax errors detected**
  * `php -l src/footballweb/app/Views/apostas/relatorio_eficiencia.php` ➔ **No syntax errors detected**
* **Validação de Banco de Dados**:
  * Confirmada a consistência de 13 apostas no dia 19/09/2026 (6 de Handicap e 7 de Cartões).
  * Com o filtro padrão `'all'`, ambas as modalidades são computadas com precisão matemática.

---

## Blindagem do Gatekeeper de Cartões: Migração de Regras de Partidas Conflituosas do AH e Reclassificação Disciplinar

---

### 1. Motivação e Diagnóstico Técnico
Ao analisar a partida **Volos NFC vs AEK Athens FC** (`fixture_id = 1593323`, Super League 1 da Grécia), o usuário identificou que o sistema aprovou uma aposta em **Total de Cartões: Menos de 5.5 Cartões @ 1.75** (Betano), enquanto na Árvore de Decisão visual a recomendação indicava **UNDER 6.5**.

#### Causas Raízes Identificadas:
1. **Deflator Geográfico Distorcido:**
   * A Super League 1 da Grécia (ID 197) e a Süper Lig da Turquia (ID 203) estavam erroneamente enquadradas no pacote de ligas europeias tradicionais (`0.82x`), desenvolvido para competições de baixo atrito disciplinar (como Premier League e Bundesliga).
   * A soma das médias das equipes era de **5.40 cartões/jogo** e o árbitro (Fotios Polychronis) possuía média de **4.40 cartões/jogo**. A aplicação do multiplicador europeu `0.82x` derrubou artificialmente a expectativa de cartões de **4.94 para 4.31**, induzindo o modelo a considerar a linha Under 5.5 matematicamente viável.
2. **Ausência de Aferição de Partidas Conflituosas no Motor de Cartões:**
   * O motor de **Handicap Asiático** já dispunha de regras consagradas para detectar cenários de alta tensão (Caldeirão da Degola, Duelo de Crises, Disparidade Técnica Extrema e Divergência de Mando).
   * O motor de **Cartões** operava de forma puramente aritmética, ignorando que um mandante afundado na degola (Volos, 11º colocado no Relegation Group) contra um gigante favorito (AEK Athens, odd 1.32 vs 9.25) apela para faltas táticas reiteradas de contenção e cartões por frustração.

---

### 2. Arquivos e Trechos Modificados

1. **`scripts/cards_engine.py`**:
   * **Reclassificação Disciplinar (`get_league_card_multiplier`)**:
     * Grécia (197) e Turquia (203) removidas do deflator europeu `0.82x` e classificadas no novo grupo de **Ligas Mediterrâneas / Balcânicas de Alto Atrito Disciplinar** com multiplicador `1.05x` e sobredispersão `1.20`.
   * **Nova Função Canônica `evaluate_cards_conflict_scenario`**:
     * Migração dos 4 cenários de partidas conflituosas do AH:
       1. **Caldeirão da Degola / Sobrevivência do Mandante**: Veto total a estratégias Under (`NO_BET`).
       2. **Duelo de Crises (Colapso Mútuo U5J)**: Veto total a estratégias Under (`NO_BET`).
       3. **Disparidade Técnica Extrema / Massacre**: Veto sumário à linha curta Under 5.5.
       4. **Divergência de Mando / Conflito de Mercado**: Veto sumário à linha curta Under 5.5.
   * **Integração em `evaluate_best_card_under_line` e `compute_fixture_expected_cards`**:
     * Repasse de metadados contextuais (`odd_home`, `odd_away`, `home_rank`, `away_rank`, `home_zone`, `home_ppg`, `standings_motivation_score`) e bloqueio estruturado de linhas.
2. **`scripts/football_ingest_trends.py`**:
   * Alinhada a função local `get_league_card_multiplier` com os mesmos multiplicadores mediterrâneos (Grécia e Turquia com `1.05x, 1.20`).
3. **`scripts/criar_apostas_cartoes_diario.py` e `scripts/alerta_cartoes_prejogo_rapido.py`**:
   * Repasse de `fixture_dict=fix` na invocação do Gatekeeper de cartões para alimentar as checagens contextuais.
4. **`src/footballweb/app/Controllers/ApostaController.php`**:
   * Sincronização do método `evaluateGatekeeper` para Total de Cartões: consulta metadados em `fixtures_trends` e replica as regras de Caldeirão da Degola, Disparidade Técnica Extrema e Divergência de Mando (Regra de Ouro nº 11).
5. **`src/footballweb/app/Views/football/dashboard.php`**:
   * Harmonizada a função visual `getBetDecisionTree()` para bloquear a exibição de Under 5.5 em jogos com disparidade técnica e alinhar o teto com o Python.

---

### 3. Impacto Prático e Regras de Negócio
| Cenário | Comportamento Anterior | Novo Comportamento Blindado |
| :--- | :--- | :--- |
| **Ligas da Grécia e Turquia** | Recebiam redutor europeu 0.82x (cartões subestimados) | **Recebem multiplicador 1.05x e sobredispersão 1.20** (expectativa realista de jogo combativo) |
| **Volos NFC vs AEK Athens FC** | Expectativa calculada em 4.31 cartões; aprovou Under 5.5 | **Expectativa recalculada para 4.94 cartões; detectado Caldeirão da Degola e disparidade técnica ➔ STATUS GK: NO_BET (Abstenção Mandatória)** |
| **Aposta Confirmada #4412 (Usuário)** | Risco de deleção indevida | **Preservada 100% intacta no banco de dados (`confirmada = 1`) para liquidação financeira real na súmula** |
| **Jogos de Conflito Técnico Futuros** | Vulneráveis a estouros em linhas curtas Under 5.5 | **Bloqueio preventivo pelo Gatekeeper (proteção absoluta do capital da banca)** |

---

### 4. Validação e Testes
* **Verificação Sintática Python e PHP**:
  * `python3 -m py_compile scripts/cards_engine.py scripts/criar_apostas_cartoes_diario.py scripts/football_ingest_trends.py scripts/alerta_cartoes_prejogo_rapido.py` ➔ **Sucesso (Código 0)**
  * `php -l src/footballweb/app/Controllers/ApostaController.php` ➔ **No syntax errors detected**
  * `php -l src/footballweb/app/Views/football/dashboard.php` ➔ **No syntax errors detected**
* **Teste com a Partida 1593323 (Volos vs AEK)**:
  * Novo xC: **4.94 cartões**.
  * Classificação de Conflito: `🚨 [Gatekeeper Cartões NO_BET / Caldeirão da Degola - Sobrevivência do Mandante]`.
  * Status: `NO_BET` / `Sem Entrada (Abstenção)`.
* **Checagem de Integridade da Aposta #4412**:
  * Registro mantido com status `Pendente`, `confirmada: 1` e valor inalterado.

---

## Evolução do Revalidador Local de Cards (`revalidar_cards_cache_local.py`) para Plataforma Universal (AH + Cartões)

---

### 1. Motivação e Diagnóstico Técnico
O script `scripts/revalidar_cards_cache_local.py`, apesar do nome genérico, limitava-se a reprocessar o mercado de Handicap Asiático. Diante da necessidade operacional de reavaliar em lote os cards de forma offline e rápida utilizando o cache MySQL (Regra nº 1), o script foi expandido para atuar como **Revalidador Universal de Cards** (Handicap Asiático e Total de Cartões Under).

---

### 2. Implementações Realizadas
* **Novo Seletor de Mercado via CLI (`--market {all, ah, cards}`)**:
  * Permite ao desenvolvedor/operador revalidar ambos os mercados em lote (`all`), ou focar exclusivamente em Handicap (`ah`) ou Cartões (`cards`).
* **Módulo de Revalidação de Cartões (`revalidate_fixture_cards`)**:
  * Integração com `cards_engine.py`: reavalia `compute_fixture_expected_cards`, sobredispersão, multiplicadores regionais e as 4 regras de partidas conflituosas com `allow_api=False`.
  * Atualiza colunas `fixtures_trends.prediction_text` e `fixtures_trends.over_cards_probability`.
* **Proteção Absoluta de Apostas Confirmadas (`--sync_apostas`)**:
  * Implementada a trava `is_conf = (confirmada == 1) or (tem_debito > 0)`.
  * Apostas confirmadas com dinheiro real ou com débito em conta corrente são 100% preservadas e intocáveis. Apenas apostas de simulação não-confirmadas (`confirmada = 0`) são atualizadas ou canceladas pelo Gatekeeper.

---

### 3. Validação e Testes
* **Verificação Sintática Python**:
  * `python3 -m py_compile scripts/revalidar_cards_cache_local.py` ➔ **Sucesso (Código 0)**
* **Simulação Pontual (Fixture #1593323 - Volos vs AEK)**:
  * Executado com `--fixture_id 1593323 --market all --sync_apostas --dry_run`.
  * Retornou `NO_BET` para AH e Cartões, e confirmou no log:
    `🔒 [Cartões Aposta Confirmada Mantida] Bet #4412 possui confirmação/débito. Imutabilidade preservada.`
* **Simulação em Lote de 76 Partidas**:
  * Executado com `--date 2026-09-20 --days 0 --market all --dry_run`.
  * Processadas com sucesso 76 partidas em menos de 3 segundos, com consumo rigorosamente ZERO de chamadas para APIs externas.



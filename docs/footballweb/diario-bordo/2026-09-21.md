# Diário de Bordo - 21/09/2026

## Saneamento Estrutural do Pipeline de Cartões, Regra Oficial de Peso 2 para Cartão Vermelho e Reconciliação Contábil

---

### 1. Motivação e Diagnóstico Técnico

Identificou-se uma anomalia grave na liquidação da partida **San Lorenzo 0 x 2 Boca Juniors (`fixture_id = 1493149`)**, realizada em 20/09/2026 às 14:45. A partida foi exibida com o status `Processamento: ✅ Completo`, porém os quantitativos de cartões amarelos e vermelhos constavam como `🟨 0-0`, escanteios `0-3` e xG `0.09-0.07`, resultando na liquidação indevida da aposta #4403 (`Menos de 6.5 Cartões`) como `Ganha` com base na justificativa errônea:
`"FT | Total Cartões Jogo: 0 < Limite 6.5 -> Aposta GANHA"`.

#### Diagnóstico da Causa Raiz:
1. **Gravação Prematura de Zeros na Ingestão (`football_ingest_trends.py`)**:
   - Ao transicionar partidas recém-encerradas para `FT`, a API-Sports (`/fixtures/statistics`) frequentemente ainda não tabulou as estatísticas detalhadas, retornando uma lista vazia (`response: []`).
   - O código não tratava a lista vazia adequadamente: pulava a inserção no `match_statistics_cache`, porém executava a query de atualização em `fixtures_trends` com as variáveis locais inicializadas em `0` (`yellow_cards_home = 0`, `yellow_cards_away = 0`, etc.).
   - Além disso, a instrução `ON DUPLICATE KEY UPDATE` carimbava `cards_api_checked_at = NOW()` de forma incondicional para status terminal (`FT`), mesmo com cartões nulos.
   - Como as colunas deixavam de ser `NULL` e passavam a ser `0`, a rotina de varredura periódica (`WHERE yellow_cards_home IS NULL`) nunca mais selecionava essas partidas para enriquecimento com a súmula final.
2. **Falha de Fallback e Persistência Incompleta no Processador de Apostas (`processar_apostas_encerradas.py`)**:
   - O método `fetch_real_fixture_cards_api` gravava exclusivamente o time mandante no `match_statistics_cache`, omitindo o time visitante.
   - Na função `get_fixture_stats`, quando a chamada à API retornava `None` (dados em cooldown ou ainda não prontos), as variáveis locais de cartões não eram anuladas. O sistema mantinha o valor `0` lido do banco, computava `total_cards = 0` e liquidava a aposta como se o jogo tivesse de fato terminado sem nenhum cartão.
3. **Ponderação Oficial de Cartões Esportivos (Regra do Cartão Vermelho)**:
   - No mercado de cartões, a regra canônica das casas de apostas (Betano, Bet365, etc.) estipula que **1 Cartão Vermelho conta como 2 Cartões** (`Total = Amarelos + Vermelhos * 2`). Os scripts estavam somando cartões vermelhos com peso unitário (`Amarelos + Vermelhos * 1`).
   - Na partida San Lorenzo x Boca Juniors, a súmula real teve 5 amarelos e 1 vermelho direto. Pela regra oficial (`5 + 1*2 = 7 cartões`), a linha `Menos de 6.5 Cartões` foi **PERDIDA** (7 > 6.5), e não Ganha.

---

### 2. Arquivos e Trechos Modificados

1. **[`scripts/processar_apostas_encerradas.py`](file:///root/datalake-air-flow-delta/scripts/processar_apostas_encerradas.py)**:
   - Em `fetch_real_fixture_cards_api`: persistência de ambos os times (`home` e `away`) e escanteios no `match_statistics_cache`.
   - Em `get_fixture_stats`: proteção contra falsos zeros. Se os cartões ainda não estão consolidados e a API retorna `None`, anula explicitamente as variáveis locais (`yellow_home = None`, `total_cards = None`), mantendo a aposta em estado `Pendente`.
   - Implementação da regra do cartão vermelho com peso 2:
     ```python
     total_cards = (yellow_home or 0) + (yellow_away or 0) + ((red_home or 0) * 2) + ((red_away or 0) * 2)
     cards_home = yh + (rh * 2)
     cards_away = ya + (ra * 2)
     ```
   - Em `evaluate_bet`: detalhamento da memória de cálculo com discriminação de amarelos e vermelhos com peso 2.

2. **[`scripts/processar_apostas_cartoes_encerradas.py`](file:///root/datalake-air-flow-delta/scripts/processar_apostas_cartoes_encerradas.py)**:
   - Sincronização dos mesmos critérios de cache duplo (mandante e visitante), proteção de status `Pendente` e contagem de cada vermelho valendo 2 cartões em `ensure_fixture_card_stats` e `evaluate_cards_under_bet`.

3. **[`scripts/football_ingest_trends.py`](file:///root/datalake-air-flow-delta/scripts/football_ingest_trends.py)**:
   - Na sincronização de partidas passadas (`FT`): se a resposta da API vier vazia (`not st_data`), o script agora incrementa `cards_api_retry_count` e **não** sobrescreve `fixtures_trends` com zeros, preservando o estado `NULL` para reconsulta no ciclo seguinte.
   - Na query principal (`ON DUPLICATE KEY UPDATE`): `cards_api_checked_at` só é carimbado com `NOW()` se houver cartões ou eventos comprovados.

4. **[`src/footballweb/app/Views/football/dashboard.php`](file:///root/datalake-air-flow-delta/src/footballweb/app/Views/football/dashboard.php)**:
   - Ajustada a variável `$hasDetailedStats` para exigir que haja cartões confirmados (`$totalCardsConfirmed > 0`), eventos registrados ou confirmação sem falhas da API (`cards_api_retry_count === 0`). Evita a exibição prematura de `Processamento: ✅ Completo`.

5. **[`scripts/revisar_corrigir_partidas_apostas_ontem.py`](file:///root/datalake-air-flow-delta/scripts/revisar_corrigir_partidas_apostas_ontem.py)**:
   - Script automatizado de auditoria, enriquecimento retroativo e reliquidação contábil.

---

### 3. Impacto Prático e Regras de Negócio

1. **Blindagem contra Liquidações Fantasmas**:
   - Nenhuma aposta do mercado de cartões poderá mais ser liquidada com base em contagens zeradas falsas decorrentes de atraso na súmula da API. A aposta permanece rigorosamente como `Pendente` até a consolidação oficial.
2. **Precisão Matemática da Regra de Cartões (1 Vermelho = 2)**:
   - Alinhamento total com as regras oficiais de mercado. Partidas com expulsões agora refletem a real sinistralidade no modelo Under Cartões.
3. **Auditoria Contábil e Livro-Razão (`conta_corrente`)**:
   - Para as apostas #4403, #4407 e #4414 que haviam sido creditadas indevidamente como Ganhas, foram gerados lançamentos auditáveis de estorno (`tipo = 'ESTORNO_APOSTA'`), subtraindo R$ 16,70, R$ 17,20 e R$ 28,00 respectivamente.
   - O saldo da conta corrente do usuário foi reconciliado com precisão de R$ 46,60 para **-R$ 15,30**.
4. **Reconciliação das Metas Diárias (`metas_diarias_cache`)**:
   - Os indicadores do dia 20/09/2026 foram recalculados:
     - **Greens**: 5
     - **Reds**: 8 (3 apostas retificadas para Red)
     - **Pushes**: 2
     - **Lucro Líquido Real**: -R$ 75,30
     - **Status do Dia**: `STOP_LOSS_ATINGIDO` (acionamento correto da proteção de banca).

---

### 4. Validação e Testes

- Execução com sucesso do script `revisar_corrigir_partidas_apostas_ontem.py`.
- **Partida 1493149 (San Lorenzo x Boca Juniors)**:
  - `match_statistics_cache`: time 451 (8 cantos, 3 amarelos, 0 vermelhos) e time 460 (2 cantos, 2 amarelos, 1 vermelho) persistidos.
  - `fixtures_trends`: `yellow_cards_home = 2`, `yellow_cards_away = 3`, `red_cards_home = 1`, `red_cards_away = 0`, `corners_home = 2`, `corners_away = 8`, `xg_home = 0.21`, `xg_away = 2.15`.
  - Aposta #4403: `status = 'Perdida'`. Detalhe: `"FT | Total Cartões Jogo: 7 (5 Amarelos + 1 Vermelhos [peso 2]) >= Limite 6.5 -> Aposta PERDIDA"`.
- **Integridade Contábil**: Lançamentos IDs 661, 662 e 663 confirmados na tabela `conta_corrente`.
- **Dashboard e Metas**: Cache do dia 20/09 atualizado e sincronizado com o MySQL.

---

## Revisão e Saneamento Retrospectivo Integral de Trends e Apostas (Datas <= 19/09/2026)

### 1. Motivação e Diagnóstico Histórico
Após a correção da partida do dia 20/09/2026 e a estabilização dos scripts estruturais, foi realizada uma varredura completa de todo o histórico registrado na base de dados para datas anteriores ou iguais a 19/09/2026.
O objetivo foi auditar se partidas anteriores haviam sofrido com zeros precoces nas súmulas, se a regra de que **1 Cartão Vermelho vale como 2 Cartões** alterava a liquidação de apostas passadas e garantir a reconciliação do livro-razão contábil (`conta_corrente`) e do sistema de metas diárias (`metas_diarias_cache`).

### 2. Universo Processado e Ações Executadas
1. **Varredura e Enriquecimento de Estatísticas**:
   - **227 apostas de cartões** analisadas no histórico ($\le$ 19/09/2026), abrangendo **174 partidas distintas** (`fixture_id`).
   - **71 partidas** já contavam com estatísticas completas de ambos os times armazenadas no banco (Cache-First).
   - **103 partidas** foram consultadas de forma controlada na API-Sports via HTTP GET com respeito a rate-limits.
   - Foram consolidados os quantitativos oficiais de cartões amarelos, vermelhos, escanteios, chutes e xG para ambos os times em `match_statistics_cache` e `fixtures_trends`.
2. **Reliquidação de Apostas com Ponderação Oficial (1 Vermelho = 2 Cartões)**:
   - Foram retificadas **24 apostas** que estavam com status inconsistente frente às estatísticas oficiais e à regra de peso 2 para cartões vermelhos (ex: Independ. Rivadavia x Fluminense, Celje x Slovan Bratislava, Pafos x Dinamo Tirana, Alaves x Villarreal).
   - Apostas canceladas/adiadas (`PST`/`CANC`) foram confirmadas como `ANULADA` com reembolso, e apostas com encerramento voluntário pelo usuário (`Cashout`) foram integralmente preservadas.
3. **Reconciliação Financeira no Livro-Razão (`conta_corrente`)**:
   - Para as apostas confirmadas que foram revertidas de Ganha para Perdida (#3236, #3246, #3249), foram lançados formalmente os estornos com `tipo = 'ESTORNO_APOSTA'` nos valores de -R$ 17,50, -R$ 17,50 e -R$ 18,80.
   - O saldo do usuário Paulo Nascimento (#558) foi ajustado de -R$ 15,30 para **-R$ 69,10**.
   - **Auditoria de Divergência**: Saldo em `usuario` (R$ -69,10) = Soma de todas as movimentações de `conta_corrente` (R$ -69,10). Divergência zero (R$ 0,00).
4. **Recálculo em Cascata do Cache de Metas Diárias (`metas_diarias_cache`)**:
   - Foram recalculados os indicadores consolidados de **30 datas de referência** históricas (de 16/08/2026 até 19/09/2026).
   - Todas as datas agora refletem fielmente o quantitativo de greens, reds, pushes, total apostado, lucro líquido e o status do dia (`META_BATIDA`, `STOP_LOSS_ATINGIDO`, `SUPERAVITARIO` ou `DEFICITARIO`).

### 3. Arquivos Criados
- **[`scripts/revisar_corrigir_partidas_apostas_historico.py`](file:///root/datalake-air-flow-delta/scripts/revisar_corrigir_partidas_apostas_historico.py)**: Script autônomo, idempotente e seguro para revisão retroativa completa de trends, apostas e metas.

---

## Nova DAG Automatizada: Revalidação Universal de Cards em Cache Local (`revalidar_cards_cache_local_dag`)

### 1. Motivação e Diagnóstico
Identificou-se a necessidade de eliminar qualquer hiato ou descompasso temporal entre a ingestão preliminar de cotações (`football_ingest_trends.py`) e a tomada de decisão estrutural do Gatekeeper nos motores analíticos (`asian_handicap_engine.py` e `cards_engine.py`). 
Casos em que cotações são registradas antes da consolidação de médias ou onde travas anti-emboscada (como a *Sobrevivência do Mandante / Caldeirão da Degola*) precisam reavaliar os cards em aberto exigiam uma esteira automatizada periódica.

### 2. Arquitetura e Implementação
1. **Zero Consumo de API (Regra 1 - Cache-First)**:
   - A DAG executa o script `scripts/revalidar_cards_cache_local.py`, consumindo **estritamente o cache local do MySQL** (`fixtures_trends`, `match_statistics_cache`, `team_moving_averages`, `team_last5_cache`, `referee_stats`), sem consumir cotas da API-Sports ou The Odds API.
2. **Agendamento e Performance**:
   - **Arquivo**: [`src/dags/revalidar_cards_cache_local_dag.py`](file:///root/datalake-air-flow-delta/src/dags/revalidar_cards_cache_local_dag.py)
   - **Periodicidade**: A cada 30 minutos (`schedule_interval='*/30 * * * *'`).
   - **Tempo de Execução**: ~1,13 segundo para processar e sincronizar todas as partidas em aberto de hoje e de amanhã.
3. **Validação**:
   - DAG registrada, desserializada e ativada (`unpause`) no Apache Airflow.
   - Execução de teste manual disparada via CLI com retorno de sucesso imediato (`state: success`).

---

## Funcionalidade de Despinamento Persistente de Alertas (Sininho e Pop-up Toast)

### 1. Motivação e Diagnóstico Técnico
O usuário reportou que alertas pinados de alta criticidade (como o alerta de **Stop Loss Diário**) continuavam sendo reexibidos na forma de pop-up flutuante (*toast stack*) a cada recarregamento de página (*page load*), mesmo após o usuário ter tomado ciência do aviso.
A investigação identificou três fatores:
1. **Ausência de Estado de Fixação no Banco**: A tabela `notificacoes_usuario` não possuía uma coluna para controlar o estado fixado/pinado (`pinada`), presumindo que todo `STOP_LOSS_DIARIO` deveria ser sempre pinado.
2. **Seleção Irrestrita no Controller (`ApostaController.php`)**: O método `getNotificacoesNaoLidas()` retornava o alerta de Stop Loss sem filtrar se o usuário já o havia dispensado ou marcado como lido.
3. **Escopo Temporário do Cache de Sessão**: O controle do pop-up dependia de `sessionStorage`, que é descartado ao fechar abas ou iniciar nova navegação, além de verificar genericamente `tipo === 'STOP_LOSS_DIARIO'` no frontend.

### 2. Arquitetura da Solução e Arquivos Modificados
Implementou-se uma esteira estrutural de despinamento com dupla camada de persistência (Banco de Dados + LocalStorage do navegador):

1. **Camada de Dados (MySQL)**:
   - Adicionada a coluna `pinada TINYINT(1) NOT NULL DEFAULT 0` na tabela `notificacoes_usuario`.
   - Inicializados os alertas pendentes existentes com `pinada = 1`.
2. **Modelo de Metas (`MetaDiariaModel.php`)**:
   - No método `verificarEGerarAlertaStopLoss()`, novos alertas de Stop Loss Diário agora são inseridos explicitamente com `'pinada' => 1`.
3. **Rotas e Controller (`Routes.php` e `ApostaController.php`)**:
   - Criada a rota `POST /notificacoes/despinar/(:num)` associada a `ApostaController::despinarNotificacao/$1`.
   - O método `despinarNotificacao($id)` atualiza atomicamente `pinada = 0` e `lida = 1` para o usuário autenticado.
   - O método `getNotificacoesNaoLidas()` passa a ordenar as notificações com prioridade absoluta para alertas pinados e não lidos (`(pinada = 1 AND lida = 0)`), e apenas alimenta `stop_loss_notif` se o alerta ainda estiver de fato pinado e não lido.
4. **Interface e Frontend (`header.php`)**:
   - **Pop-up Toast Flutuante**: Adicionado botão de ação `Despinar` no corpo do card do Stop Loss e vinculação no fechamento.
   - **Dropdown do Sininho**: Itens pinados exibem a tag `📌 PINADO` e um botão direto `Despinar`, permitindo remover a fixação com 1 clique sem abrir a tela de metas.
   - **Persistência Imediata**: A função JavaScript `despinarAlerta(id)` grava imediatamente em `localStorage` (`notif_despinada_${id} = 1`), impedindo reexibição em qualquer reload subsequente, e dispara a requisição AJAX ao backend para sincronização no MySQL.

### 3. Validação e Testes
- Verificação de sintaxe PHP em `Routes.php`, `ApostaController.php`, `MetaDiariaModel.php` e `header.php` (todos sem erros).
- Verificação do registro de rotas no CodeIgniter (`spark routes`) confirmando a rota `POST notificacoes/despinar/([0-9]+)`.
- Verificação da coluna `pinada` e registros no banco de dados MySQL na tabela `notificacoes_usuario`.

---

## Correção Estrutural de Status Diário (Déficit) e Liquidação Automática de Partidas Adiadas (PST)

### 1. Motivação e Diagnóstico Técnico
O usuário reportou que na tabela **"Histórico de Fechamento Diário"** (`/metas`) alguns dias passados apareciam com o status `⏳ Em curso`, apesar de se tratarem de datas com todas as partidas já encerradas e resultados definidos.
A investigação revelou duas causas raízes sistêmicas:
1. **Omissão do Status `DEFICITARIO` na Camada Visual (`metas/index.php`)**:
   - Quando um dia encerra com todas as apostas liquidadas (`pendentes = 0`) e saldo negativo que não rompe o teto do Stop Loss (ex: perdas controladas de -R$ 3,40 em 14/09, -R$ 4,10 em 10/09 e -R$ 4,00 em 28/08), o modelo `MetaDiariaModel.php` classifica o dia corretamente como `DEFICITARIO`.
   - Na view `metas/index.php`, a cadeia condicional não tratava `DEFICITARIO`, fazendo com que caísse no bloco `else`, renderizando indevidamente o badge `⏳ Em curso` (e no card superior `⏳ Em Andamento`).
2. **Partidas Adiadas/Canceladas Mantidas Eternamente como Pendentes**:
   - No script `processar_apostas_encerradas.py`, quando uma partida recebia status oficial de adiamento (`PST`, `CANC`, `POSTPONED`, etc.), o código apenas imprimia uma mensagem no log e mantinha a aposta como `Pendente`.
   - Isso ocorreu na aposta #4271 (*Levante vs Athletic Club* em 16/09/2026, `fixture_id = 1570389`), partida adiada pela La Liga (`PST`), impedindo a liquidação definitiva e mantendo `pendentes_count = 1` no dia 16/09.

### 2. Arquivos e Trechos Modificados
1. **[`src/footballweb/app/Views/metas/index.php`](file:///root/datalake-air-flow-delta/src/footballweb/app/Views/metas/index.php)**:
   - Adicionada a classe CSS `.status-deficitario { background: rgba(249, 115, 22, 0.2); color: #fb923c; border: 1px solid rgba(249, 115, 22, 0.4); }`.
   - Incluído o tratamento `elseif ($statusDia === 'DEFICITARIO')` no card de Desempenho Diário (`📉 Deficitário`).
   - Incluído o tratamento `elseif ($hSt === 'DEFICITARIO')` na tabela do Histórico de Fechamento Diário (`📉 Déficit`).
2. **[`scripts/processar_apostas_encerradas.py`](file:///root/datalake-air-flow-delta/scripts/processar_apostas_encerradas.py)**:
   - Quando `status_curr in ('PST', 'CANCELLED', 'POSTPONED', 'CANC', 'ABD')`, a aposta é liquidada como `novo_status = 'ANULADA'`, com reembolso integral do valor apostado (`valor_computado = valor_aposta`), gravação no banco e acionamento de `creditar_retorno_aposta`.
3. **[`scripts/processar_apostas_cartoes_encerradas.py`](file:///root/datalake-air-flow-delta/scripts/processar_apostas_cartoes_encerradas.py)**:
   - Sincronizado o mesmo tratamento para partidas de cartões com status de cancelamento/adiamento (`PST`, `CANC`, `POSTPONED`, `CANCELLED`, `ABD`), liquidando como `ANULADA` com reembolso integral.

### 3. Validação e Reconciliação Contábil
- Execução do processamento de liquidação:
  - Aposta #4271 liquidada como `ANULADA` com reembolso de R$ 10,00 creditado na conta corrente do usuário (#558).
  - Saldo da conta corrente atualizado de R$ -69,10 para **R$ -59,10** (Divergência extrato vs saldo: R$ 0,00).
- Recálculo do cache de metas diárias para 16/09/2026:
  - `pendentes_count` zerado (de 1 para 0).
  - Status do dia consolidado como `STOP_LOSS_ATINGIDO` (1 Green, 3 Pushes, 3 Reds, Lucro -R$ 139,00).
- Todos os dias anteriores a hoje (21/09/2026) encontram-se rigorosamente com zero pendências e status consolidados (`META_BATIDA`, `SUPERAVITARIO`, `STOP_LOSS_ATINGIDO` ou `DEFICITARIO`), eliminando qualquer ocorrência de `⏳ Em curso` para dias passados.

---

## Totalização do Rodapé no Histórico de Fechamento Diário (`metas/index.php`)

### 1. Motivação e Requisito
Solicitação do usuário para totalizar formalmente as colunas no rodapé (`<tfoot>`) da tabela **"Histórico de Fechamento Diário"** (tela `/metas`):
- Greens, Reembolsos, Reds, Odd Média, Total Investido, Lucro Líquido e ROI.

### 2. Implementação Técnica
1. **[`src/footballweb/app/Views/metas/index.php`](file:///root/datalake-air-flow-delta/src/footballweb/app/Views/metas/index.php)**:
   - Adicionada a estilização CSS `.history-table tfoot tr.history-table-total-row` e `.history-table tfoot td` para destacar a linha final com borda superior dupla, tipografia em negrito e fundo contrastante.
   - Implementado acumulador durante o loop de `$historico` para computar a soma de apostas cadastradas, greens, reembolsos, reds, valor total investido e lucro líquido consolidado.
   - Cálculo da **Odd Média Global Ponderada** ($\frac{\sum \text{odd} \times \text{apostas}}{\sum \text{apostas}}$) e do **ROI Consolidado** ($\frac{\text{Lucro Total}}{\text{Investimento Total}} \times 100\%$).
   - Inserção da seção `<tfoot>` com badges e estilização verde/vermelha para retornos financeiros.

### 3. Validação
- Verificação de sintaxe PHP (`php -l src/footballweb/app/Views/metas/index.php`) com zero erros detectados.
- Teste com 30 dias de histórico comprovando exatidão aritmética dos somatórios e cálculo do ROI e Odd Média.
